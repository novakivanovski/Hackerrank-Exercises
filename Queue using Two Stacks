q = []
type_q = []
num_queries = int(input())
for i in range(num_queries):
    command = list(map(int, input().split(" ")))
    if command[0] == 1:
        q.append(command[1])
    elif command[0] == 2:
        q.pop(0)
    else:
        print (q[0])
